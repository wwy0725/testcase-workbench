# 兜底卡片调整

## 20260806

### 【兜底卡片】兜底卡片调整

##### 校验兜底卡片类型统一为"兜底"
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_001 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 用户对话未匹配到具体功能

- 用户对话未匹配到任何功能
  - 展示兜底卡片，卡片类型统一显示为"兜底"
  - 不再区分"全部应用"和"企业服务"两种兜底类型
- 用户对话匹配到具体功能
  - 不展示兜底卡片，直接展示对应功能结果

##### 校验发票分析卡片展示正确
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_002 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 触发发票分析兜底场景

- 触发发票分析兜底卡片展示
  - 卡片名称显示为"发票分析"
  - 固定文案展示：可以使用【发票分析】服务，支持按客户、供应商、货物、地区多维度开展发票专项分析。
  - 卡片样式为 [icon] 发票分析 [去查看]
- 点击发票分析卡片
  - 跳转至全部应用页面
  - 自动打开【查发票】抽屉

##### 校验开票设置卡片展示正确
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_003 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 触发开票工具兜底场景

- 触发开票工具兜底卡片展示
  - 卡片名称显示为"开票设置"
  - 卡片样式为 [icon] 开票设置 [去查看]
- 点击开票设置卡片
  - 跳转至全部应用页面
  - 自动打开【找工具】>【更多设置】抽屉

##### 校验业财工具卡片展示正确
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_004 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 触发业财工具兜底场景

- 触发业财工具兜底卡片展示
  - 卡片名称显示为"业财工具"
  - 卡片样式为 [icon] 业财工具 [去查看]
- 点击业财工具卡片
  - 跳转至全部应用页面
  - 自动打开【找工具】>【业财工具】抽屉

##### 校验记账进度卡片展示正确
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_005 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards、/api/itcrmweb/progress/getBookkeepingProgress | 测试数据: 存在/不存在记账进度数据

- 查询到记账进度，当前月记账进行中
  - 文案展示：当前X月记账进行中。可以使用【查报表】服务获取最新收入成本情况~
  - 卡片样式为 [icon] 查报表 [去查看]
- 查询到记账进度，当前月记账已完成
  - 文案展示：当前X月记账已完成。可以使用【查报表】服务获取最新收入成本情况~
  - 卡片样式为 [icon] 查报表 [去查看]
- 未查询到记账进度
  - 展示拒答话术：不好意思呀，暂未查询到相关数据。
- 点击查报表卡片
  - 跳转至查报表服务

##### 校验报税进度卡片展示正确
> 优先级: 高 | 关联需求: REQ_兜底卡片调整_006 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards、/api/itcrmweb/progress/getTaxProgress | 测试数据: 存在/不存在报税进度数据

- 查询到报税进度，当前月申报进行中
  - 文案展示：当前X月申报进行中。可以使用【查税金】服务获取最新的纳税明细~
  - 卡片样式为 [icon] 查税金 [去查看]
- 查询到报税进度，当前月申报已完成
  - 文案展示：当前X月申报已完成。可以使用【查税金】服务获取最新的纳税明细~
  - 卡片样式为 [icon] 查税金 [去查看]
- 未查询到报税进度
  - 展示拒答话术：不好意思呀，暂未查询到相关数据。
- 点击查税金卡片
  - 跳转至查税金服务

##### 校验新增发票更新兜底卡片
> 优先级: 中 | 关联需求: REQ_兜底卡片调整_007 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 触发发票更新兜底场景

- 触发发票更新兜底卡片展示
  - 卡片名称显示为"发票更新"
  - 卡片样式为 [icon] 发票更新 [去查看]
- 点击发票更新卡片
  - 执行正确的跳转行为（跳转至发票更新相关页面）

##### 校验新增发票抬头兜底卡片及关联问题
> 优先级: 中 | 关联需求: REQ_兜底卡片调整_008 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 触发发票抬头兜底场景

- 触发发票抬头兜底卡片展示
  - 卡片名称显示为"发票抬头"
  - 卡片样式为 [icon] 发票抬头 [去查看]
- 验证关联问题配置
  - 关联问题包含"查一下我的开票抬头"
- 点击关联问题"查一下我的开票抬头"
  - 触发对应功能，正确跳转或展示结果
- 点击发票抬头卡片
  - 执行正确的跳转行为（跳转至发票抬头相关页面）

##### 校验多兜底卡片组合展示
> 优先级: 中 | 关联需求: REQ_兜底卡片调整_009 | 前置条件: 调用的接口: /api/itcrmweb/chat/getFallbackCards | 测试数据: 同时触发多个兜底场景

- 同时触发多个兜底卡片
  - 所有匹配的兜底卡片均展示
  - 卡片排列顺序符合产品规则
  - 各卡片样式独立、无重叠或错位
