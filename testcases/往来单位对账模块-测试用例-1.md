# 往来单位对账

## 往来单位对账模块

### 20260818

#### 【往来单位对账】往来单位对账模块

##### 模块显示 - 往来单位≠0时显示模块

> 优先级: P1 | 关联需求: 往来单位对账模块显示规则 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 往来单位数量>0的账套

- 账套中存在待收/待付/待开票/待收票数据，进入模块查看页面
  - 模块正常展示在页面中
  - 模块内按默认顺序显示各tab卡片
- 账套中仅存在一种类型数据（如仅待收），进入页面
  - 模块正常展示
  - 仅显示有待收tab，其余tab不显示或显示空态

##### 模块显示 - 往来单位=0时隐藏模块

> 优先级: P1 | 关联需求: 往来单位对账模块显示规则 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 往来单位数量=0的账套

- 账套中无任何待收/待付/待开票/待收票数据，进入页面
  - 往来单位对账模块不展示
  - 页面布局无模块占位空白

##### 状态显示 - 待对账状态展示

> 优先级: P2 | 关联需求: 对账状态展示 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在待对账状态的tab

- 查看模块中状态为"待对账"的tab卡片
  - 状态文案显示"待对账"
  - 状态样式符合设计稿（颜色/图标）

##### 状态显示 - 已暂存状态展示

> 优先级: P2 | 关联需求: 对账状态展示 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在已暂存状态的tab

- 查看模块中状态为"已暂存"的tab卡片
  - 状态文案显示"已暂存"
  - 状态样式符合设计稿（颜色/图标）

##### 状态显示 - 已对账状态展示

> 优先级: P2 | 关联需求: 对账状态展示 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在已对账状态的tab

- 查看模块中状态为"已对账"的tab卡片
  - 状态文案显示"已对账"
  - 状态样式符合设计稿（颜色/图标）

##### 操作交互 - 待对账显示"去对账"

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在待对账状态的tab

- 查看状态为"待对账"的tab卡片
  - 操作项显示"去对账"
  - 操作项可点击，样式符合设计稿

##### 操作交互 - 已对账显示"查看"

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在已对账状态的tab

- 查看状态为"已对账"的tab卡片
  - 操作项显示"查看"
  - 操作项可点击，样式符合设计稿

##### 操作交互 - 已暂存显示"继续对账"

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在已暂存状态的tab

- 查看状态为"已暂存"的tab卡片
  - 操作项显示"继续对账"
  - 操作项可点击，样式符合设计稿

##### 跳转定位 - 点击"去对账"跳转并定位tab

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule、/api/itcrmweb/contact/reconciliationDetail（待Walle确认） | 测试数据: 存在待对账状态的tab（如待收）

- 点击"待收"tab上状态为"待对账"的"去对账"操作项
  - 页面跳转到"对账处理"页面
  - 自动定位到"待收"tab
  - 对账处理页面数据正确加载
- 点击其他tab（如待付）上状态为"待对账"的"去对账"操作项
  - 页面跳转到"对账处理"页面
  - 自动定位到对应的"待付"tab

##### 跳转定位 - 点击"查看"跳转并定位tab

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule、/api/itcrmweb/contact/reconciliationDetail（待Walle确认） | 测试数据: 存在已对账状态的tab

- 点击tab上状态为"已对账"的"查看"操作项
  - 页面跳转到"对账处理"页面
  - 自动定位到对应的tab
  - 对账处理页面展示已对账数据

##### 跳转定位 - 点击"继续对账"跳转并定位tab

> 优先级: P1 | 关联需求: 操作交互设计 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule、/api/itcrmweb/contact/reconciliationDetail（待Walle确认） | 测试数据: 存在已暂存状态的tab

- 点击tab上状态为"已暂存"的"继续对账"操作项
  - 页面跳转到"对账处理"页面
  - 自动定位到对应的tab
  - 对账处理页面展示暂存数据，可继续编辑

##### 显示顺序 - 默认按指定顺序展示

> 优先级: P2 | 关联需求: 显示顺序规则 | 前置条件: 调用的接口: /api/itcrmweb/contact/queryReconciliationModule（待Walle确认） | 测试数据: 存在多种类型数据的账套

- 账套中同时存在待收、其他待收、待付、其他待付、待开票、待收票数据，查看模块
  - 各tab按以下顺序从上到下展示：待收 → 其他待收 → 待付 → 其他待付 → 待开票 → 待收票
- 仅存在部分类型数据（如仅待收、待付、待开票），查看模块
  - 存在的tab按默认顺序展示
  - 不存在的tab不显示或显示空态
  - 顺序保持相对位置不变

##### 查询对账模块信息接口

> 优先级: P2 | 关联需求: 往来单位对账模块数据获取 | 前置条件: 无 | 测试数据: 各状态组合的账套数据

- 请求接口，往来单位数量>0
  - 返回各tab的对账状态（待对账/已暂存/已对账）
  - 返回各tab对应的数量
  - 返回各tab对应的操作项类型（去对账/查看/继续对账）
  - 返回数据按默认顺序排列
- 请求接口，往来单位数量=0
  - 返回空数据或moduleShow=false标识

##### 查询对账状态详情接口

> 优先级: P2 | 关联需求: 对账处理页面数据加载 | 前置条件: 无 | 测试数据: 指定tab和状态的数据

- 传入tab类型（如待收）和状态（如待对账），请求接口
  - 返回对应tab和状态下的对账明细数据
  - 数据结构包含对账所需字段
- 传入已对账状态，请求接口
  - 返回已对账的历史数据
  - 数据不可编辑
- 传入已暂存状态，请求接口
  - 返回暂存的草稿数据
  - 数据可继续编辑
