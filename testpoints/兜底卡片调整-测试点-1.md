# 兜底卡片调整 - 测试点

## 1. 兜底卡片展示规则

### 1.1 兜底卡片类型切换
- 验证"全部应用"和"企业服务"兜底卡片统一改为"兜底"类型
- 验证特殊场景（发票分析、开票工具、业财工具、记账进度、报税进度、发票更新、发票抬头）使用独立卡片样式

### 1.2 兜底卡片样式
- 验证卡片样式为 [icon] 名称 [去查看]
- 验证固定文案展示正确

---

## 2. 发票分析卡片

### 2.1 卡片展示
- 验证卡片名称为"发票分析"
- 验证固定文案展示：可以使用【发票分析】服务，支持按客户、供应商、货物、地区多维度开展发票专项分析。
- 验证卡片样式：[icon] 发票分析 [去查看]

### 2.2 点击跳转
- 验证点击卡片后跳转至全部应用
- 验证跳转后自动打开【查发票】抽屉

---

## 3. 开票工具卡片

### 3.1 卡片展示
- 验证卡片名称为"开票设置"
- 验证卡片样式：[icon] 开票设置 [去查看]

### 3.2 点击跳转
- 验证点击卡片后跳转至全部应用
- 验证跳转后自动打开【找工具】>【更多设置】抽屉

---

## 4. 业财工具卡片

### 4.1 卡片展示
- 验证卡片名称为"业财工具"
- 验证卡片样式：[icon] 业财工具 [去查看]

### 4.2 点击跳转
- 验证点击卡片后跳转至全部应用
- 验证跳转后自动打开【找工具】>【业财工具】抽屉

---

## 5. 记账进度卡片

### 5.1 卡片展示 - 有进度数据
- 验证记账进行中时文案：当前X月记账进行中。可以使用【查报表】服务获取最新收入成本情况~
- 验证记账已完成时文案：当前X月记账已完成。可以使用【查报表】服务获取最新收入成本情况~
- 验证卡片样式：[icon] 查报表 [去查看]

### 5.2 卡片展示 - 无进度数据
- 验证未查询到进度时展示拒答话术：不好意思呀，暂未查询到相关数据。

### 5.3 点击跳转
- 验证点击卡片后跳转至查报表服务

---

## 6. 报税进度卡片

### 6.1 卡片展示 - 有进度数据
- 验证申报进行中时文案：当前X月申报进行中。可以使用【查税金】服务获取最新的纳税明细~
- 验证申报已完成时文案：当前X月申报已完成。可以使用【查税金】服务获取最新的纳税明细~
- 验证卡片样式：[icon] 查税金 [去查看]

### 6.2 卡片展示 - 无进度数据
- 验证未查询到进度时展示拒答话术：不好意思呀，暂未查询到相关数据。

### 6.3 点击跳转
- 验证点击卡片后跳转至查税金服务

---

## 7. 新增兜底卡片 - 发票更新

### 7.1 卡片展示
- 验证新增"发票更新"兜底卡片
- 验证卡片样式正确

### 7.2 点击行为
- 验证点击发票更新卡片的跳转行为

---

## 8. 新增兜底卡片 - 发票抬头

### 8.1 卡片展示
- 验证新增"发票抬头"兜底卡片
- 验证卡片样式正确

### 8.2 关联问题
- 验证"查一下我的开票抬头"作为发票抬头的关联问题
- 验证点击关联问题能正确触发对应功能

### 8.3 点击行为
- 验证点击发票抬头卡片的跳转行为

---

## 9. 兜底卡片触发场景

### 9.1 意图识别匹配
- 验证用户输入匹配到功能时，不展示兜底卡片
- 验证用户输入未匹配到功能时，展示兜底卡片

### 9.2 多卡片组合展示
- 验证多个兜底卡片同时展示时的排列和样式
